name: mobiusamza
description: "A new Flutter project."
publish_to: 'none'
version: 0.1.0

environment:
  sdk: '>=3.3.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  flutter_pdfview: ^1.3.2
  foundation: ^0.0.5
  http: ^1.2.1
  json_path: ^0.7.1
  path_provider: ^2.1.2
  provider: ^6.1.2
  syncfusion_flutter_pdfviewer: ^25.1.40
  tuple: ^2.0.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
